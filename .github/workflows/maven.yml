name: Basic CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env: 
  SERVICE_NAME: OrderService
  DOCKER_REGISTRY: registry
  DOCKER_IMAGE: docker-image
  JAVA_VERSION: '17'
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: ${{ env.JAVA_VERSION }}
        distribution: 'temurin'
        cache: maven
      
    - name: Build Order Service
      run: |
        cd  ${{env.SERVICE_NAME}}
        mvn clean install
       
    - name: Success Message
      run: echo "✅ Order services built successfully!"
  build-docker:
        
      
